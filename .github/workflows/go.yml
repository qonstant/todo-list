name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_USER: todo-user
          POSTGRES_PASSWORD: password
          POSTGRES_DB: todo-db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: ^1.22

      - name: Install Docker
        run: |
          sudo apt-get update
          sudo apt-get install docker.io -y

      - name: Build Docker image
        run: docker build -t todo-list .

      - name: Start PostgreSQL service
        run: docker compose up -d postgres

      - name: Wait for PostgreSQL to be ready
        run: |
          timeout 15 bash -c 'until docker exec todo-db pg_isready; do sleep 1; done'

      - name: Apply migrations
        run: make migrateup

      - name: Test
        run: make test
